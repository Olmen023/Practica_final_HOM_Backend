{
  "name": "bildyapp-api",
  "version": "1.0.0",
  "description": "BildyApp — API REST para gestión de albaranes (Práctica Final)",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node --env-file=.env src/index.js",
    "dev": "node --watch --env-file=.env src/index.js",
    "test": "cross-env NODE_ENV=test node --experimental-vm-modules node_modules/jest/bin/jest.js --forceExit --detectOpenHandles",
    "test:watch": "npm test -- --watch",
    "test:coverage": "npm test -- --coverage",
    "lint": "echo \"(sin linter configurado)\""
  },
  "keywords": [
    "express",
    "mongodb",
    "jwt",
    "swagger",
    "socket.io",
    "albaranes"
  ],
  "author": "Olmen023",
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "dependencies": {},
  "devDependencies": {}
}
